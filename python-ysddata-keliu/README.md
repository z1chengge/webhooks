## 关于此 Skill

- 使用python 连接到 SQL Server 2008 数据库 调用数据库存储过程
- 需要在环境变量中设置 `连接地址、数据库名称、用户名、密码`
- 仅返回 `status`、`columns`、`rows` 这三项 JSON，适合直接导入 Bitable 或其它数据可视化工具

### 安装

```bash
cd C:\Users\Administrator\.openclaw\workspace
clawhub install python-ysddata-keliu